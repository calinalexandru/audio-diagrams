import styled from '@emotion/styled';

export default styled.button`
  font-family: inherit;
  border: 0;
  background: #ccc;
  height: 40px;
  width: auto;
  cursor: pointer;
  margin: 0 5px;

  &:hover {
    background: #fff;
  }
`;
